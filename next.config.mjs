/** @type {import('next').NextConfig} */
//next.config.mjs 是 Next.js 的配置文件，用于设置 Next.js 应用的配置选项。
const nextConfig = {
    experimental: {
      appDir: true, // 启用 App Router
    },
  };
  
  export default nextConfig;
  